spring.application.name=first-springboot-app

# ===============================
# Database Connection
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/manage_user?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=zyyx3678
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# Hibernate / JPA
# ===============================
# T? ??ng c?p nh?t schema (create, create-drop, update, validate, none)
spring.jpa.hibernate.ddl-auto=update

# Dialect cho MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Show SQL ra console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
